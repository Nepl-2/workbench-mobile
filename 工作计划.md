# 手机网页版工作媒介 — 工作计划

> 目标：为 DeepSeek Harness 智能体（本软件）做一个手机网页版工作媒介，打通内外网，多风格界面，好用的细节。

## 一、需求拆解

| # | 需求 | 说明 |
|---|------|------|
| 1 | 查看近期对话 | 会话列表 + 打开历史对话 |
| 2 | 下载已完成的工作内容 | 列出/下载产出文件 |
| 3 | 上传内容 | 上传文件给智能体 |
| 4 | 与智能体对话 | 发消息 + 流式回复 |
| 5 | 切换模型 | 列出模型 + 切换 |
| 6 | 内外网可用 | 通过 Zerotier One 打通，内外网同一地址可访问 |
| 7 | 多风格界面 | 参考 1Panel 里 copaw(云岫/钉钉风) + hermes-agent(深色控制台风) |
| 8 | 用户体验细节 | 见下文 UX 清单 |

## 二、架构决策

**方案：独立手机 PWA（前端）+ Node 桥接服务（后端）**

理由：DSH 现有 Web GUI 是 React 全功能客户端，但它通过自定义 Typert RPC + WebSocket 下联协议与后端通信，且 `dsh web --host 0.0.0.0` 官方"未做鉴权前不支持"。与其改这个复杂 monorepo，不如做一个自包含的移动前端 + 一个薄桥接层：

```
手机浏览器(PWA) ──HTTPS/SSE──▶ 桥接服务(Node) ──▶ DSH 会话API(聊天/历史)
                              ├─▶ 工作目录文件系统(下载/上传/文件列表)
                              └─▶ DSH 设置(模型列表/切换)
```

- **前端**：纯静态 PWA（HTML/CSS/JS），移动优先，多主题，无构建依赖，可离线壳。
- **桥接服务**：Node 原生实现（无 node_modules 依赖），一个进程同时：托管前端静态文件 + 提供 REST/SSE API + 对接 DSH。
- **文件/模型**：直接读工作目录与设置文件（不依赖 DSH RPC）。
- **聊天/历史**：对接 DSH 会话 API（最小 RPC 客户端）。

## 三、分阶段执行计划

### 阶段 0：基础侦查 ✅（已完成）
- 摸清 DSH 架构：`dsh web`（127.0.0.1:3080）、`/api` RPC + WebSocket 下联、`--host/--port/--trusted-host` 参数、ACP(仅自动化无历史)、SDK(stdio)。
- 摸清 1Panel 资源：copaw(8088, 云岫钉钉风)、hermes-agent(9119, 深色控制台)。
- 摸清 Zerotier：本机未安装 zerotier-cli，需安装 + 网络 ID。

### 阶段 1：手机 PWA 前端（本次优先，零依赖可独立完成）
- `index.html` + `styles.css` + `app.js` + `manifest.webmanifest`。
- 4 个 Tab：对话 / 会话历史 / 文件 / 设置。
- 多主题（参考 1Panel AI 聊天）：
  - 「云岫·钉钉风」浅色气泡、圆角、拟微信/钉钉聊天
  - 「Hermes·深色」深色控制台、卡片化、科技蓝
  - 「水墨·松岭风」浅米宣纸、水墨点缀（沿用你之前的审美）
  - 「极简白」纯净信息流
- UX 细节：流式打字、自动滚底+回顶部、发送中骨架、语音友好的大按钮、触控防误触、深色模式跟随系统、下拉刷新会话、文件大小/时间、断线重连提示、PWA 可"添加到主屏幕"。

### 阶段 2：桥接服务（Node，无第三方依赖）
- 静态托管前端。
- REST+SSE 接口：`/api/sessions`、`/api/chat`(SSE)、`/api/files`、`/api/files/download`、`/api/upload`、`/api/models`、`/api/models/switch`。
- 文件/模型：文件系统 + 设置读写。
- 聊天/历史：对接 DSH `/api`（实现最小 RPC 客户端，读取 Typert 描述符）。
- 兜底：若 RPC 对接过脆，回退为"复用 DSH 客户端 bundle + 注入移动主题 CSS"。

### 阶段 3：Zerotier 内外网打通（需你配合）
- 安装 zerotier-one（需 root/sudo）。
- 加入你的 Zerotier 网络（需你提供 16 位网络 ID）。
- 桥接服务监听 0.0.0.0 或 Zerotier 网卡 IP。
- 手机端装 Zerotier One 加入同一网络 → 内外网同一地址访问。
- 写 systemd 服务 + 开机自启 + 部署说明。

### 阶段 4：联调与交付
- 本地联调（对话/历史/文件/上传/模型切换）。
- Zerotier 下用手机实测。
- 打包交付（部署脚本 + README）。

## 四、需要你提供的信息
1. **Zerotier 网络 ID**（16 位十六进制，如 `8286ac0e47xxxxxx`）—— 加入你的网络用。
2. **安装权限**：本机安装 zerotier-one 需要 root；你能提供 sudo 密码，或愿意自己跑一行命令吗？

## 五、风险与说明
- DSH 官方对"远程无鉴权访问"持保留态度；Zerotier 是私有虚拟局域网，风险可控，但我会把桥接服务只绑定在 Zerotier 网段 + 加一个简单访问口令。
- 聊天/历史对接是最不确定的一环，已备好兜底方案。
